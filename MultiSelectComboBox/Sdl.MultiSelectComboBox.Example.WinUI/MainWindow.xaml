<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Sdl.MultiSelectComboBox.Example.WinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Sdl.MultiSelectComboBox.Example.WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:sdl="using:Sdl.MultiSelectComboBox.Controls"
    xmlns:models="using:Sdl.MultiSelectComboBox.Example.Models"
    xmlns:converters="using:Sdl.MultiSelectComboBox.Example.WinUI.Converters"
    Title="Sdl.MultiSelectComboBox.Example.WinUI">

    <!--<Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>-->

            <!--<converters:BoolToVisibilityConverter x:Key="BoolToVis" />

            <Style x:Key="Custom.Button.Style" TargetType="Button">
                <Setter Property="Foreground" Value="RoyalBlue"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderBrush" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="HorizontalContentAlignment" Value="Right"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="VerticalAlignment" Value="Bottom"/>
                <Setter Property="HorizontalAlignment" Value="Right"/>
                <Setter Property="Padding" Value="1"/>
                <Setter Property="Margin" Value="0"/>
            </Style>-->

    <Grid Margin="15">
        <!--<Grid.Resources>
            <ResourceDictionary>
                <ResourceDictionary.MergedDictionaries>
                    <ResourceDictionary Source="Styles/MultiSelectComboBox.Custom.ControlTemplate.xaml"/>
                    <ResourceDictionary Source="Styles/MultiSelectComboBox.Custom.DropdownItemTemplate.xaml"/>
                    <ResourceDictionary Source="Styles/MultiSelectComboBox.Custom.SelectedItemTemplate.xaml"/>
                </ResourceDictionary.MergedDictionaries>
            </ResourceDictionary>
        </Grid.Resources>-->

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="5" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Margin="0,0,2,25" MinWidth="150" Width="Auto" >
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Grid.Row="0" Text ="Items" Margin="0,5,0,0" HorizontalAlignment="Left" FontWeight="SemiBold" Width="120" Height="21" VerticalAlignment="Top"/>
                    <StackPanel Grid.Column="1" Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,5,0,0" Height="21" VerticalAlignment="Top">
                        <TextBlock Text="{x:Bind ViewModel.SelectedItemsCount, Mode=OneWay}" FontWeight="SemiBold"/>
                        <TextBlock Text=" Selected" FontWeight="SemiBold"/>
                    </StackPanel>
                </Grid>

                <Grid Margin="0,25,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="100" MinHeight="49" MaxHeight="300" />
                        <RowDefinition Height="10" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0">

                        <sdl:MultiSelectComboBox Margin="0,0,0,20"
                                                 Visibility="Visible"
                                                 ItemsSource="{x:Bind ViewModel.Items, Mode=OneWay}"
                                                 FilterService="{x:Bind ViewModel.FilterService, Mode=OneWay}"
                                                 SuggestionProvider="{x:Bind ViewModel.CustomSuggestionProvider, Mode=OneWay}"
                                                 />
						<!--AutoCompleteService="{x:Bind ViewModel.AutoCompleteService, Mode=OneWay}"-->

						<sdl:MultiSelectComboBox Margin="0,0,0,20"
                                                 Visibility="Collapsed"
                                                 ItemsSource="{x:Bind ViewModel.Items, Mode=OneWay}"
                                                 FilterService="{x:Bind ViewModel.FilterService, Mode=OneWay}"
                                                 SuggestionProvider="{x:Bind ViewModel.CustomSuggestionProvider, Mode=OneWay}"
                                                 />

                        <Button Command="{x:Bind ViewModel.ClearItemsCommand, Mode=OneWay}" Style="{StaticResource Custom.Button.Style}"  Width="128" Height="20">
                            <TextBlock Text="Clear selected items" TextDecorations="Underline"/>
                        </Button>
                    </Grid>

                    <Border Height="2" Grid.Row="1" Margin="5,0,5,0" HorizontalAlignment="Stretch" Background="LightGray"/>

                    <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Margin="0,5,0,0">
                        <Border BorderBrush="LightGray" BorderThickness="1">
                            <Grid>

                                <TextBlock Text ="Demo Settings" Margin="9,4,0,0" HorizontalAlignment="Left" FontWeight="SemiBold" Width="95" Height="21" VerticalAlignment="Top" />

                                <TextBlock Text="Style:" Margin="27,26,0,0" HorizontalAlignment="Left" Width="48" Height="26" VerticalAlignment="Top" />

                                <Grid Margin="64,25,0,0" Height="50" VerticalAlignment="Top" HorizontalAlignment="Left" Width="210">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="19*"/>
                                        <RowDefinition Height="31*"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Orientation="Vertical" Grid.Column="0" Margin="10,6,-74,0" Grid.ColumnSpan="2" Grid.RowSpan="2">
                                        <RadioButton x:Name="RadioButtonDefaultStyle" GroupName="Style" Content="Default control template style" Margin="1" IsChecked="True" Command="{x:Bind ViewModel.StyleChangedCommand, Mode=OneWay}"/>
                                        <RadioButton x:Name="RadioButtonCustomStyle" GroupName="Style" Content="Custom control template style" Margin="1" Command="{x:Bind ViewModel.StyleChangedCommand, Mode=OneWay}"/>
                                    </StackPanel>
                                </Grid>


                                <Button Content="Select 20 random items" Command="{x:Bind ViewModel.SelectRandomItemsCommand, Mode=OneWay}" HorizontalContentAlignment="Center" VerticalContentAlignment="Top" HorizontalAlignment="Left" Margin="30,76,0,0" VerticalAlignment="Top" Grid.Column="0" Height="23" Width="184"/>

                                <TextBlock Text="Selection Mode:" HorizontalAlignment="Left" Margin="27,104,0,0" VerticalAlignment="Top" Width="97"/>
                                <ComboBox HorizontalAlignment="Left" Margin="126,106,0,0" VerticalAlignment="Top" Width="88" SelectedItem="{x:Bind ViewModel.SelectionMode, Mode=TwoWay}" ItemsSource="{x:Bind ViewModel.SelectionModes, Mode=OneWay}"/>

                                <CheckBox Content="Clear Filter Criteria as Dropdown (with Keyboard focus) is closing" IsChecked="{x:Bind ViewModel.ClearFilterOnDropdownClosing, Mode=TwoWay}" HorizontalAlignment="Left" Margin="29,142,0,0" VerticalAlignment="Top" Grid.Column="0" Width="364"/>

                                <CheckBox Content="Items in the 'Selected Items Panel' are editable" IsChecked="{x:Bind ViewModel.IsEditable, Mode=TwoWay}" HorizontalAlignment="Left" Margin="29,159,0,0" VerticalAlignment="Top" Grid.Column="0" Width="313"/>

                                <CheckBox Content="Enable/disable items (alternate)" IsChecked="{x:Bind ViewModel.EnableAlternateItems, Mode=TwoWay}" HorizontalAlignment="Left" Margin="29,176,0,0" VerticalAlignment="Top" Grid.Column="0" Width="313"/>

                                <CheckBox Content="Listen to 'Filter Text' changed" IsChecked="{x:Bind ViewModel.ListenToFilterTextChanged, Mode=TwoWay}" HorizontalAlignment="Left" Margin="29,193,0,0" VerticalAlignment="Top" Grid.Column="0" Width="314"/>
                                <CheckBox Content="Listen to 'Selected Items' changed" IsChecked="{x:Bind ViewModel.ListenToSelectedItemsChanged, Mode=TwoWay}" HorizontalAlignment="Left" Margin="29,211,0,0" VerticalAlignment="Top" Grid.Column="0" Width="314"/>

                                <CheckBox Content="Enable Autocomplete" IsChecked="{x:Bind ViewModel.EnableAutoComplete, Mode=TwoWay}" HorizontalAlignment="Left" Margin="29,229,0,0" VerticalAlignment="Top" Grid.Column="0" Width="314"/>
                                <CheckBox Content="Enable batch selection (paste support)" IsChecked="{x:Bind ViewModel.EnableBatchSelection, Mode=TwoWay}" IsEnabled="{x:Bind ViewModel.EnableAutoComplete, Mode=OneWay}" HorizontalAlignment="Left" Margin="45,247,0,0" VerticalAlignment="Top" Grid.Column="0" Width="314"/>

                                <CheckBox Content="Enable grouping" IsChecked="{x:Bind ViewModel.EnableGrouping, Mode=TwoWay}" HorizontalAlignment="Left" Margin="29,267,0,0" VerticalAlignment="Top" Grid.Column="0" Width="314"/>
                                <CheckBox Content="Use 'Recently Used' service" IsChecked="{x:Bind ViewModel.UseRecentlyUsedGroupingService, Mode=TwoWay}" IsEnabled="{x:Bind ViewModel.EnableGrouping, Mode=OneWay}"  HorizontalAlignment="Left" Margin="45,285,0,0" VerticalAlignment="Top" Grid.Column="0" Width="314"/>

                                <CheckBox Content="Enable filtering" IsChecked="{x:Bind ViewModel.EnableFiltering, Mode=TwoWay}" HorizontalAlignment="Left" Margin="30,305,0,0" VerticalAlignment="Top" Grid.Column="0" Width="314"/>
                                <CheckBox Content="Use 'Custom Filter' service" IsChecked="{x:Bind ViewModel.UseCustomFilterService, Mode=TwoWay}" IsEnabled="{x:Bind ViewModel.EnableFiltering, Mode=OneWay}" HorizontalAlignment="Left" Margin="45,323,0,0" VerticalAlignment="Top" Grid.Column="0" Width="314"/>

                                <CheckBox Content="Clear selection on filter changed (Single selection mode only)" IsChecked="{x:Bind ViewModel.ClearSelectionOnFilterChanged, Mode=TwoWay}" Margin="30,343,10,0" VerticalAlignment="Top" Grid.Column="0"/>

                                <CheckBox Content="Enable on demand service" IsChecked="{x:Bind ViewModel.EnableSuggestionProvider, Mode=TwoWay}" Margin="30,363,10,0" VerticalAlignment="Top" Grid.Column="0"/>
                            </Grid>
                        </Border>
                    </ScrollViewer>

                </Grid>

            </Grid>

            <Border Width="2" Grid.Column="1" HorizontalAlignment="Stretch" Margin="0,25,0,40" Background="LightGray"/>

            <Grid Grid.Column="2" Width="Auto" MinWidth="150" Margin="2,0,0,0" >
                <TextBlock Text="Events Log" HorizontalAlignment="Left" VerticalAlignment="Top" Padding="0,5,0,0" FontWeight="Medium" Height="26" />
                <ScrollViewer Margin="0,25,0,24" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                    <TextBox Text="{x:Bind ViewModel.EventLog, Mode=OneWay}" TextWrapping="NoWrap" IsReadOnly="True"/>
                </ScrollViewer>
                <Button Command="{x:Bind ViewModel.ClearLogCommand, Mode=OneWay}" Style="{StaticResource Custom.Button.Style}" Width="75" Height="24">
                    <TextBlock Text="Clear log" TextDecorations="Underline"/>
                </Button>
            </Grid>
        </Grid>
    </Grid>
</Window>
